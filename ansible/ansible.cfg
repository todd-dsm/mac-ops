# Ansible configuration for macops-ansible project
[defaults]
inventory = inventory/localhost
roles_path = ./roles
host_key_checking = False
fact_caching = jsonfile
fact_caching_connection = /tmp/ansible_facts
fact_caching_timeout = 3600

[ssh_connection]
pipelining = True
